apiVersion: v1
kind: Service
metadata:
  name: {{ include "soft-arch.fullname" . }}
  labels:
    {{- include "soft-arch.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "soft-arch.selectorLabels" . | nindent 4 }}
