apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "soft-arch.fullname" . }}-test-connection"
  labels:
    {{- include "soft-arch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "soft-arch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
